{
  "name": "laundry-backend",
  "version": "1.0.0",
  "description": "Laundry Management System Backend",
  "main": "server.js",
  "scripts": {
    "start": "npx json-server --watch db.json --port 10000",
    "dev": "npx json-server --watch db.json --port 10000",
    "init-db": "echo 'JSON Server - No initialization needed'"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
